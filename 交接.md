# Agent Runtime Map · 交接文档

写给换机器后接手的人（很可能是未来的你自己，或者下一个 AI 会话）。

本文只写**别处没写的东西**：当前确切状态、换机器怎么做、以及**还没做完的事**。
架构、数据模型、开发规则见 [docs/CC_HANDOFF.md](docs/CC_HANDOFF.md)（550 行，英文，最权威）。

最后核实时间：2026-09-01。文中每个数字都是当时跑命令查出来的，不是回忆。

---

## 一、这是什么

把 AI Agent 代码库变成**有证据支撑、可逐步查看**的执行地图。

和市面上"让 LLM 读仓库然后画图"的工具的根本区别：**拓扑由确定性 AST 分析器提取，不是模型写出来的**。图里不可能出现代码中不存在的连线，同一个 commit 永远生成同一张图，每个节点和连线都带 `文件:行号` 证据和置信度。可选的 LLM 层只能改名和补描述，结构上没有能力新增任何节点或连线。

这条原则不可妥协。任何改动如果削弱它，就是做错了方向。

---

## 二、当前状态（2026-09-01 实测）

### 已发布

| | |
|---|---|
| npm | `agent-runtime-map`、`@agent-runtime-map/react`、`@agent-runtime-map/mcp` —— 三个包均 **0.8.3**，`latest` 均指向它 |
| GitHub | https://github.com/TheCrazyAnt/agent-runtime-map （**公开**，0 star） |
| 最新 Release | [v0.8.3](https://github.com/TheCrazyAnt/agent-runtime-map/releases/tag/v0.8.3)，附三个 tarball |
| 浮动标签 | `v1` → `e8e299d`（= `v0.8.3` 同一 commit） |
| main | `5161997`，比 v0.8.3 多一个纯文档提交 |
| 工作树 | 干净，无未提交改动 |
| 待处理 | 0 个 issue；**3 个 Dependabot PR**（#40 lucide-react 大版本跳跃、#28 开发依赖组、#5 elkjs） |

### 两个容易踩的坑

1. **`v1` 不会自动跟随 main。** 它是手动移动的浮动标签，用户通过 `TheCrazyAnt/agent-runtime-map@v1` 消费。每次发布后必须手动 `git tag -f v1 <commit> && git push -f origin v1`，否则用户拿到的还是旧版。

2. **v0.8.1 有 tag 但没有 GitHub Release。** 历史遗留，无影响（因为 action 现在从 npm 安装而不是从 Release 资产下载），但看 Release 列表时会发现 0.8.2 直接跳到 0.8.0。

---

## 三、换机器怎么做

### 拷贝前先删这些

```bash
rm -rf node_modules apps/viewer/node_modules
```

**这一步是必须的，不是优化。** 文件夹 220MB 里 196MB 是 `node_modules`，且含 **darwin-arm64 原生二进制**（fsevents、lightningcss、rollup、rolldown）。新机器只要不是 Apple Silicon Mac，viewer 构建会立刻失败。删掉后只剩约 21MB。

顺手也可以删（都会重新生成）：`.DS_Store`、`.logic-map/`、各 `dist/`、`*.tsbuildinfo`。

### 新机器上

```bash
npm ci && npm run build && npm run check
```

前置条件：

- **Node 20 或 22**（`engines` 要求 `>=20`，CI 跑 20/22）
- **python3 ≥ 3.10** 在 PATH 上 —— Python 适配器需要。**这个版本下限没写在任何文档里**：`adapters/python/scripts/extract.py:95` 用了 `ast.Match`，那是 3.10 才有的。旧版 macOS 自带的 python3 是 3.9，会在分析时抛 `AttributeError`。当前这台机器用的是 Anaconda 的 3.13.5。
- 无需 Rust、编译器或任何 postinstall 原生构建

### 账号侧（不跟着文件夹走）

```bash
gh auth login
git config --global user.name "tangyishun9846"
git config --global user.email "tangyishun9846@gmail.com"
```

`npm login` **不需要**——自动发布走 Trusted Publishing（OIDC），不用本地凭证。只有手动 `npm publish` 才需要，而正常流程不该手动发布。

仓库里**没有任何** `.env`、`.npmrc`、私钥或绝对路径（已逐项确认）。

### 其实 clone 也一样

`.git` 完整（7.7MB，origin 已配好），所以 `git clone` 得到的树和拷贝文件夹完全相同。拷贝唯一的好处是省去重新认证。

---

## 四、发布流程

完整版见 [docs/RELEASING.md](docs/RELEASING.md)（中英双语）。要点：

1. **七处版本声明必须一致** —— 4 个 `package.json` + `cli.ts`/`mcp/server.ts` 的 `VERSION` 常量 + `action.yml` 的 `cli-version` 默认值。`npm run release:check` 和一个测试都会拦截不一致。

   最后一处不是形式：action 会为每个消费者从 npm 安装 `agent-runtime-map@<cli-version>`。**v0.8.1 就是栽在这里**——tag 打了、npm 发了，但没建 GitHub Release，而当时 action 是从 Release 资产下载的，URL 404。所以后来改成从 npm 装了。

2. 开 PR → **八项 CI 全绿** → 合并 → `git tag vX.Y.Z <merge-commit>` → 推送 tag 自动触发 OIDC 发布 → 建 GitHub Release → **最后**移动 `v1`。

3. 任何一步失败就停，`v1` 不动，消费者不受影响。

### npm Trusted Publisher 配置（账号侧，换机器不影响）

三个包各配过一次，参数相同：

| 字段 | 值 |
|---|---|
| Organization | `TheCrazyAnt` |
| Repository | `agent-runtime-map` |
| Workflow | `npm-publish.yml` |
| Environment | 留空 |

npm 账号已开 2FA + 安全密钥。**如果将来换 npm 账号或重建仓库，这三处要重新配**，否则自动发布会失败。

---

## 五、还没做完的事

**没有全部弄完。** 以下都是核实过的，不是凭印象列的。

### A. 双语本地化审计的遗留项

做双语语义时跑过一轮对抗审计，40 项确认缺陷修了约 25 项。**确认仍未修**的：

| 问题 | 位置 | 影响 |
|---|---|---|
| **LLM 语义层对 feature 无效** | `packages/semantic/src/index.ts:197-205` | node 路径已修（写入 `semantic`），但 feature 路径仍写旧的 `label`/`description`。Viewer 读 `feature.semantic`，所以模型给 feature 的改名**看不见，但它压低的 confidence 照样生效**——正是 node 路径当初修掉的那个 bug |
| **MCP 只出英文，描述还是原始的** | `packages/mcp/src/summaries.ts` | 标签已用 `semantic.label.en`，但 `describeStep`/`describeFeature`/`describeEvidence` 打印的仍是原始 `description`。结果是业务标签配技术描述。且无 locale 参数，中文用户拿不到中文 |
| **report.html 的 file:// 兜底显示技术名** | `packages/core/src/continuous.ts:696-724` | HTTP 打开走 Viewer 显示业务名；双击本地文件走 `FALLBACK_SCRIPT`，显示 `feature.label` 等原始字段。同一份报告两种读法 |
| **`SemanticLabel.evidence` 不分语言** | `packages/schema/src/index.ts:262-275` | `label`/`description`/`labelSource`/`confidence` 都是按 locale 分开的，只有 `evidence` 是一个扁平数组（`localization.ts:106` 在 locale 循环外声明）。中文名来自文档、英文名来自标识符时，读者分不清哪条证据支撑哪个语言 |
| **Pages 部署步骤从未真正执行** | `.github/workflows/action-e2e.yml` | `map-pages` job 跑到 artifact 为止就停了——这是**故意的**，真部署会把本仓库的内部地图公开发布。但意味着 `actions/deploy-pages@v5` 在 action 自己的路径上零执行覆盖。（`demo-pages.yml` 里有一次真部署，但它不走复合 action，是手写的步骤） |

### B. 路线图上的开放项（已核实仍然开放）

- **compiler 是严格 1:1** —— 每个 logic 节点只来自一个 raw 节点（`rawNodeIds[0]`），"把多个底层节点聚成一个逻辑阶段"没做。要动这个得先拆掉边映射对 `rawNodeIds[0]` 的假设。
- **没有 jsdom** —— `vitest.config.ts` 无 `environment`，devDeps 里没有 jsdom/happy-dom。所有 Viewer 测试都是纯函数测试。历史上有两个 bug（0.3.0 的高亮失效、0.3.2 的搜索崩溃）就是这个缺口放过去的。
- **Viewer 没有消费 `<LogicMap />`** —— `apps/viewer/src/App.tsx` 直接 import `@xyflow/react` 自己组装，与发布的组件是两套代码。
- **CLI 无法摄入 trace** —— `applyTraceEvents` 只在 `packages/react` 里，嵌入方能用，CLI 用不了；也没有 OpenTelemetry 适配器。
- **框架适配** —— NestJS 和 Mastra 目前**只在 `detectFrameworks()` 里做了 package.json 名称识别**，没有装饰器/注册表分析。Next.js 只支持 App Router 的 `route.ts`，没有 server actions 和 `middleware.ts`。Python 的 Django 同理，只有标签没有 `urlpatterns` 解析。
- **Python 适配器** —— 调用只在已分析集合内按名解析，不走 import 图；类方法归属、LangGraph/CrewAI Python 约定都没做。

### C. ⚠️ `docs/ROADMAP.md` 已经过期，会误导人

核实发现它和 `CC_HANDOFF.md §9` 互相矛盾。**这三个未勾选项其实早就做完了**：

- `[ ] Web Component` —— 已发布，`packages/react/src/element.tsx` 的 `<logic-map>`
- `[ ] high-level embeddable component` —— 已发布，只是叫 `<LogicMap />` 不叫 `<AgentRuntimeMap />`
- `[ ] Python AST adapter` —— 已发布，`adapters/python/`

还有一项是半开放：`[ ] saved layouts and annotations` —— **saved layouts 已做**（`App.tsx` 的 `LAYOUT_PREFIX`，按项目持久化、支持撤销和重置），annotations 完全没做。应该拆成两条。

**接手第一件事建议：把 ROADMAP.md 和 CC_HANDOFF §9 对齐。** 现在照着 ROADMAP 干活会重复造已有的轮子。

---

## 六、不可妥协的产品原则

改任何东西前先读这几条（完整版见 `docs/CC_HANDOFF.md §1` 和 `§10`）：

- **代码是唯一事实来源。** LLM 永远不能创造拓扑。
- **确定性优先于语义。** 同一 commit + 同一配置必须产出同一张图。
- **不确定的必须标出来。** 读不出来就标 `待确认`，不能猜。中文要求所有 token 都能解析，否则宁可不给名字——因为"创建 DirectorLedger"这种中英混杂正是双语功能要消灭的东西。
- **playback 是静态推演，永远不是实时运行。** 不能让读者误以为在看真实请求。
- **布局要保留空间记忆** —— 切换功能、展开、聚焦、播放时都不能重跑 ELK。
- 默认不调 LLM、不上传源码、不发遥测、不执行被分析的项目。

---

## 七、常用命令

```bash
npm run check              # typecheck + 全部测试 + 构建
npm run release:check      # 上面这些 + audit + 三个包校验 + 版本一致性
node scripts/benchmark-report.mjs   # 分析准确率报告（TP/FP/FN）
npm run dev -- <项目路径>   # 从源码跑 CLI 看效果
```

测试目前 **188 个**（本机 Python 3.9 环境下 python-adapter 那 4 个会挂，CI 上正常）。

---

## 八、给下一个 AI 会话的话

这个项目的历史里，几乎每个真正的 bug 都是**在浏览器里或用真实数据验证时**发现的，而不是靠读代码想出来的：搜索框把整个 Viewer 弄白、feature 切换时节点乱跳、documented capability 把三个无关步骤重命名成同一个词、发布脚本在 registry 尚未同步时误判失败。

所以：**改完要真的跑起来看**。写了测试不等于验证过。

另外，本项目对"诚实"的要求高于对"完整"的要求。宁可标 `待确认`、宁可报 `CALL_UNRESOLVED_DYNAMIC`，也不要给一个看起来合理但没有证据的答案。
