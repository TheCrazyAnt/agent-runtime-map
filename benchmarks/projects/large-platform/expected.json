{
  "notes": "Hand-confirmed answer for a project that does not fit its budget. 66 routes and ~150 business nodes are extracted; 44 may be drawn. This file says which 44 the map owes a reader — the four chains whole, not the four chains' front doors. Raw extraction accuracy is the four small samples' job (see scope.exactRawTopology); every claim below is about what survives compression.",
  "options": {
    "maxNodes": 44
  },
  "scope": {
    "exactRawTopology": false
  },
  "diagnostics": {
    "required": [
      {
        "code": "LOGIC_GRAPH_COMPRESSED"
      }
    ]
  },
  "logic": {
    "requiredNodes": [
      { "type": "entrypoint", "label": "POST /api/drafts/:id/publish" },
      { "type": "workflow", "label": "Publish" },
      { "type": "ai_process", "label": "Editorial Review" },
      { "type": "tool", "label": "Moderation Tool" },
      { "type": "human_gate", "label": "Approve Publication" },
      { "type": "data", "label": "Article Data" },

      { "type": "entrypoint", "label": "POST /api/ingest" },
      { "type": "workflow", "label": "Ingest" },
      { "type": "tool", "label": "Embed Tool" },
      { "type": "external_system", "label": "api.openai.com" },
      { "type": "data", "label": "Chunk Data" },

      { "type": "entrypoint", "label": "GET /api/search" },
      { "type": "tool", "label": "Ranker Model" },
      { "type": "external_system", "label": "search.internal.example.com" },

      { "type": "workflow", "label": "Digest" },
      { "type": "ai_process", "label": "Digest Writer" },
      { "type": "external_system", "label": "api.sendgrid.com" }
    ],
    "maxDanglingEntrypoints": 4
  },
  "featureCount": {
    "min": 4,
    "max": 24
  },
  "features": [
    {
      "entry": { "type": "entrypoint", "label": "POST /api/drafts/:id/publish" },
      "health": "healthy",
      "variants": [
        {
          "orderedRoute": [
            "POST /api/drafts/:id/publish",
            "Publish Draft",
            "Publish",
            "Approve Publication"
          ],
          "controls": [
            { "from": "Publish", "to": "Approve Publication", "control": "human_approval" }
          ]
        },
        {
          "orderedRoute": [
            "POST /api/drafts/:id/publish",
            "Publish Draft",
            "Publish",
            "Moderation Tool"
          ]
        },
        {
          "orderedRoute": [
            "POST /api/drafts/:id/publish",
            "Publish Draft",
            "Publish",
            "Article Data"
          ]
        }
      ]
    },
    {
      "entry": { "type": "entrypoint", "label": "POST /api/ingest" },
      "variants": [
        {
          "orderedRoute": [
            "POST /api/ingest",
            "Ingest Document",
            "Ingest",
            "Embed Tool",
            "api.openai.com"
          ]
        },
        {
          "orderedRoute": [
            "POST /api/ingest",
            "Ingest Document",
            "Ingest",
            "Embed Tool",
            "Chunk Data"
          ]
        }
      ]
    },
    {
      "entry": { "type": "entrypoint", "label": "GET /api/search" },
      "variants": [
        {
          "orderedRoute": [
            "GET /api/search",
            "Search Articles",
            "Ranker Model",
            "search.internal.example.com"
          ]
        }
      ]
    },
    {
      "entry": { "type": "workflow", "label": "Digest" },
      "label": "Digest",
      "healthNot": "healthy",
      "diagnostics": [
        { "code": "FEATURE_ENTRY_UNRESOLVED" }
      ],
      "variants": [
        {
          "orderedRoute": [
            "Digest",
            "api.sendgrid.com"
          ]
        }
      ]
    }
  ]
}
